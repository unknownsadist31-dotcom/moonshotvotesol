{
  "name": "moonshotvote",
  "version": "1.0.0",
  "private": true,
  "description": "Moonshot vote-to-list landing page with PHP proxy backend",
  "scripts": {
    "start": "php -S 0.0.0.0:${PORT:-80} -t .",
    "dev": "php -S 127.0.0.1:3000 -t ."
  },
  "engines": {
    "php": ">=8.1"
  }
}
